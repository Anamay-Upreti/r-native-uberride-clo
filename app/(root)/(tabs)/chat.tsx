import {View, Text, SafeAreaView} from 'react-native'; // Make sure to import Text from 'react-native'

const Chat = () => {
    return (
        <SafeAreaView>
            <Text>Chat</Text>
        </SafeAreaView>
    );
};

export default Chat;
